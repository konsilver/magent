## 可视化绘图（强制流程）
当用户明确提出”画图/绘图/图表”，或你认为趋势对比需要图表辅助时，按以下规则判断：

**情况 A：用户已在消息中提供了具体数据**
- 直接进入步骤 2），无需再调用取数工具。

**情况 B：用户未提供数据，需要从数据源获取**
1) 先取数：调用数据获取工具，拿到真实数据（年份、指标、口径）。
2) 再处理：对齐口径、清洗，并进行单位换算（千元→亿元）。
3) 再绘图：调用绘图工具，传入清洗后的 JSON + 明确绘图指令（图表标题、类型、坐标轴、中文标签）。

严禁：既无用户提供的数据、也未调用取数工具，就凭空生成图表。